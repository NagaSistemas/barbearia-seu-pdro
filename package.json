{
  "name": "barbearia-backend",
  "version": "1.0.0",
  "description": "API Backend para Barbearia Seu Pedro",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && node server.js",
    "postinstall": "cd backend && npm install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "firebase-admin": "^12.0.0",
    "nodemailer": "^6.9.7"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}